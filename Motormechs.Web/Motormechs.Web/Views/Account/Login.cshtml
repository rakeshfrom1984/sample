@model Motormechs.Web.Models.LoginViewModel

@{
    ViewBag.Title = "Log in";
}
<script>
    function LoginCheck() {
        var username = jQuery("#UserName").val();
        var password = jQuery("#Password").val();
        var rememberMe = jQuery("#RememberMe").val();
        //var Login = { Username: username, Password: password, RememberMe: rememberMe }

        //jQuery.post('http://api.foofiles.com/api/Login/LoginCheck', Login, function (response) {
        //    debugger
        //    alert(response.IsSuccess)
        //    alert(response.Message)
        //    if (response.IsSuccess) { }
        //});
    }
</script>

<section id="login" class="col-md-6 form-Section">
    <h2>@ViewBag.Title</h2>
    <div class="row">
        <div class="col-md-12">
            <section id="loginForm">
                @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
                {
                    @Html.AntiForgeryToken()
                    @*<h4>Use a local account to log in.</h4>*@
                    <hr />
                    @Html.ValidationSummary(true)
                    <div class="form-group">
                        @Html.LabelFor(m => m.UserName, new { @class = "col-md-4 control-label", @style = " text-align: left;" })
                        <div class="col-xs-12" style="float:left">
                            @Html.TextBoxFor(m => m.UserName, new { @class = "form-control" })
                            @Html.ValidationMessageFor(m => m.UserName)
                        </div>
                    </div>
                    <div class="form-group">
                        @Html.LabelFor(m => m.Password, new { @class = "col-md-4 control-label", @style = " text-align: left;" })
                        <div class="col-xs-12" style="float:left">
                            @Html.PasswordFor(m => m.Password, new { @class = "form-control" })
                            @Html.ValidationMessageFor(m => m.Password)
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-xs-10">
                            <div class="checkbox">
                                @*@Html.CheckBoxFor(m => m.RememberMe, new { @class = "css-checkbox" })
                                @Html.LabelFor(m => m.RememberMe, new { @class = "css-label cb0" })*@
                                <input type="checkbox" class="css-checkbox" value="true" name="RememberMe" id="RememberMe" />
                                <label class="css-label cb0" for="RememberMe">Remember me?</label>
                                <input type="hidden" value="false" name="RememberMe" />
                            </div>
                        </div>
                    </div>
                    <div class="form-group" style="margin-top:15px">
                        <div class="col-xs-12">
                            <input type="submit" value="Log in" onclick="LoginCheck()" class="btn btn100 btn-default" />
                        </div>
                    </div>
                    <div class="form-group" style="margin-top:15px">
                        <div class="col-md-12">
                            <p>
                                <div class="pull-left"><a style="color: #008e4d" href="/home/forget-password">Forgotten</a> your Password?</div>
                                <div class="pull-right"><a style="color: #008e4d" href="/home/register">New User</a></div>
                                @*@Html.ActionLink("Register!", "Register")*@
                            </p>
                        </div>
                    </div>
                }
            </section>
        </div>
        @*<div class="col-md-4">
                <section id="socialLoginForm">
                    @Html.Partial("_ExternalLoginsListPartial", new { Action = "ExternalLogin", ReturnUrl = ViewBag.ReturnUrl })
                </section>
            </div>*@
    </div>

    @Scripts.Render("~/bundles/jqueryval")
</section>
